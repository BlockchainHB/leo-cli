/**
 * CLI Tools Index
 * 
 * Simple CLI scripts for subagents to call via Bash.
 * Each script takes arguments and outputs JSON to stdout.
 * 
 * Available scripts:
 * - perplexity-search.ts: Web search via Perplexity
 * - firecrawl-scrape.ts: Scrape a URL via Firecrawl
 * 
 * Sanity operations use the existing functions in servers/sanity/
 */

export {};

